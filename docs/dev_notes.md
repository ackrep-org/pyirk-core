# Development Notes

This document contains (maybe unsorted and outdated) notes about ongoing and future developments.
In a bigger project this information would be managed e.g. by issues but a markdown file is simpler and faster for now.



- [] i100: make `test_c011__load_mod_from_uri` executable on CI
    - generate config.toml with suitable paths
